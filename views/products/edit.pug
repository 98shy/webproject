extends ../layout

block content
  .container
    h1.main-title Edit Product
    form(action=`/products/${product.id}?_method=PUT`, method="POST")
      input(type="hidden", name="_method", value="POST")
      include _form
      button.btn.btn-outline-primary(type="submit") 완료
